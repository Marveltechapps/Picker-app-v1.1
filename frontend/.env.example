# Backend API URL. Required for Expo Go on a physical device.
# - Web / simulator: default http://localhost:3000 works.
# - Expo Go on phone: use your computer's LAN IP so the device can reach the backend.
#
# Find your IP: Windows: ipconfig  |  macOS/Linux: ifconfig or ip addr
# Example (replace with your IP): EXPO_PUBLIC_API_URL=http://192.168.1.100:3000
EXPO_PUBLIC_API_URL=http://localhost:3000
