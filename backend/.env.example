NODE_ENV=development
PORT=3000
# Bind to 0.0.0.0 so Expo Go on same LAN can reach the API (optional; default is 0.0.0.0)
# HOST=0.0.0.0
JWT_SECRET=your-jwt-secret-change-in-production

# MongoDB – required for all API routes (health works without it). Use Atlas connection string or local mongodb://localhost:27017/picker
MONGODB_URI=

# OTP/SMS – optional: path to config.json (e.g. absolute path to config (2).json). If unset, uses backend/config.json.
OTP_CONFIG_PATH=backend/config.json
# DEV MODE OTP: set to 1 or true to use random 4-digit OTP in response (no SMS, no rate/attempt limits). Also set in config.json as otpDevMode.
# OTP_DEV_MODE=1

# AWS S3 – document uploads (do not commit real credentials; use .env only)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_S3_BUCKET=selorg-picker-documents
# AWS_REGION=ap-south-1
